# Worker secrets (set via: cd worker && npx wrangler secret put SECRET_NAME)
SESSION_SECRET=           # Random 64-char hex for HMAC session tokens
ZAI_API_KEY=              # Default z.ai API key
ENCRYPTION_KEY=           # AES-256 key for encrypting user LLM API keys
ADMIN_PASSWORD_HASH=      # bcrypt hash of admin password
CHAOSBIRD_ADMIN_TOKEN=    # Krishna's Chaosbird admin token for messaging leads
