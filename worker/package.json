{
  "name": "gaigentic-hub-api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute gaigentic-hub-db --file=./schema.sql --local",
    "db:remote": "wrangler d1 execute gaigentic-hub-db --file=./schema.sql --remote"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/bcryptjs": "^2.4.6",
    "wrangler": "^4.0.0",
    "typescript": "^5.7.2"
  }
}
